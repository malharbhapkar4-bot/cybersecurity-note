Windows PowerShell

Power shell is a cross-platform task automation solution made up of a Command line shell, a Sripting language. & a Configuration management franewark

Commando in PowerShell

Gel-Content

gets the content of a file & displays. int it in the console

Set Location

Piping is used in Command line that allows the output of one command to be used cu input of In powershell piping is more powerful anomer. because it passes object from to next.

Gel-Child Item: retrives the files (as object)

& the pipe (1) sends these file objects to Sort -object by length mice ther Sorts then

Get-ChildItem | Sort-Objet Length willl Sarl the objects in length

For more objets & operater

use Room
