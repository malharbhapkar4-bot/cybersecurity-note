Windows Command line

First setting a user & the enter the password to get into the system.

You can use system info. to get information we are currently running

help- Provider help information for a specific Command 

Clears the Command prompt screen.

Network Troubleshooting

One comment troubleshooting task is checking if checking if the server can access a particular server on the internet. The pa is ping target -name Synta
