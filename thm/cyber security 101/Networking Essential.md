Networking Essential

DRCP (Dynamic Host Configuration. Brutice)) is like an automatic Seating coordinates for devices (like you phore eft) when they Connect to wi-fi of a network

Instead of Someone manually typing in an ip address for every single device mer aus. it instantly behind the scence.

DICO uses for steps known as DORA

Discover The client broadcasts a message. Searching for a local DHCP Server

2 Offes Ther server replies win available IP addreus for the client

The client responds to accept the offere 3 Request IP addresss

Acknowledge The server Confirmy to assign fraticing the Connectin

ARP (Address Resolution Protocol)

is a communication protocol used to link an iP addres (lkyer 3) to a physical MAC addres (layer), on a local network
