OSI Model

osi(open system Interconnection Modes).

Critical model provides framework how all networked devices will send receive and interpret data

↳ Layer 1 Physical

This layer references the physical Components of the hardware used networking in

↳ Layer 2 Data link

focuses any mission physical. on physical addressing of the The network It receives a packet from layer and adds MAC address of receiing

(↳ Layer 3 Network

where the magic of routing (re-ass-embly takes place

↳ OSPF Copen shortest path (first)

POUVA

OSI Model

osi(open system Interconnection Modes).

Critical model provides framework how all networked devices will send receive and interpret data

↳ Layer 1 Physical

This layer references the physical Components of the hardware used networking in

↳ Layer 2 Data link

focuses any mission physical. on physical addressing of the The network It receives a packet from layer and adds MAC address of receiing

(↳ Layer 3 Network

where the magic of routing (re-ass-embly takes place

↳ OSPF Copen shortest path (Grst)

6 Layer 4 Transport

when data is sent between devices it one of two different protocols that are des de Upon several factors

TCP Trance mission Control Protocol)

UDP ( user datagram Protocol)

TCP

4 Guarantees accuracy.

↳ reliability

Srequres relicable connection between two devices

UDP

is faster than TCP

is Control over how quickly packet is sent

4 UDP does't care if data is received

Layer 5 Session

when connection is established between two Computes session is Created

↳ Layer 6 Presentation

This Layer acts as translates for data to and from the application

The receiving Computer will also understand.

data sent for Computer in one format.

Slayer 7 Application

application layer is the layer in which protocols and miles are in place to determine how the user shol interact With data
