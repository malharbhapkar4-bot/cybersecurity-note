Packets & Frames.

→Packets & frames are small pieces of data when forming together larges piece of information.

→Packet

is piece of data from layer 3 containing info Such as IP header and payload.

→Frame

which encapsulates. is used in 2nd layer

the packet and add additional information

such as MAC addresses

TCP (The three way handshake)

Transmission Control Protocol.

very similar to the Os I model TCP consist of a layers and is arguably just a version OSI model

Application

Transport

4 Internet

4 Network Interface

TCP makes a connection btw bin sender I receiver and yes 3 way hard shake

process

SYN used to initiate connection

2) SYN/ACK packet send to to acknowledge the Syn

3 ACK

acknowledgment packer used had to know that pallet have been Successfully received.

4 UDP (User datagram protocol)

Another protocal that is used to commonicate data berveen devices.

It does not need constant Connection between devices & not and synchronisation.
