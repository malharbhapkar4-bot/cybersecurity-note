DNS in Detail

What is DNS?

Domain name system provides a simple way for us to communicate with devices on the without remembering complex numbers.

↳ Domain Hierarchy.

→TLD (Top level Domain).

sit at the root level of the Domain Name System hierarchy telling servers. where to send web. requests

Second level Domain

→ It is the specific name you register (eg. github, amazon)

→ Subdomain

It is a prefix added to the left of the main domain name (seperated by a dot) to Create a distinct separate Section

↳ DNS Recard types

→A Record

There resolve to IPV4

AAAA record

These resolve to IPVG
CNAME These records resolve to another domain name THM hay Subdomain Shopify
MIX Record.

There resolve to the address of the Sever mat handles the email fus the domain you are querying.

7X7 Record

are free text fields where any text based dale can be stored

→ what happens when you make a DNS request

I Request domain name

2 Recursive DNS server

3 1200 t DNS Semes

4 At the therapeutic
