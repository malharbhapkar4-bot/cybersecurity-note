Intro to LAN

Local Area Network (LAN) Topologies

7 star topology devices are individually connected though a Central networking device

2] Bus topology

It relies upon single backbone cable. Similar to the leaf of a tree

3 Ring, topology Devices are directly connect doo each Other to form a loop.

4 Switch

are dedicated devices within a network that are designed to aggregate multiple

Router

it's job is to connect networks and pass data between them. It does this by using routing
Is subnetting


used at place like office where 250+ deuces are connected

SARP (Address resolution protocol)

Technology that is responsible far all adding identify themself as a network &

How ARP works ?

search device within the network has to store information which is called cache

ARP Request

ARP 

DHCP (Dynamic Hast Configuration Protocol)

Used to automatically assign IP addresses essential network configuration and other parameters
