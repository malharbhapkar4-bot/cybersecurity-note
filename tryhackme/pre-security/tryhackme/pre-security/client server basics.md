Client sever

client ,Server, service

→Client sends a request

→The serves receives it, does the work, & Sends it back a response

→The service the actual capability being offered

Is Request & Response

(Client sends a request

Sever process the request

G-Server Server sends

protocols

is a set of agreed - upon rules that define how the Systems Communicate, what messages. , in what order they are look like sent

Live a language & etiquette Combined.

example IP, HTTP, HTTPS

port

is a number that identifier application or service a specific machine, so network traffic from exactly known where on that machine to go

If IP address is like address of an apartment building Port is like flat

SONS (Domain name System)

The system translates human-readable domain names to numerical IP addresses.

6s IP (Internet protocol)

Is the addressing and  routing System that lets data find it is way from one machine to another across a internet netwack

DNS = Phonebook

IP = phone number
